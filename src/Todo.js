const Todo = function (name, dueDate, priority, notes) {
  const _name = name;
  const _dueDate = dueDate;
  const _priority = priority;
  const _notes = notes;

  return {};
};

export default Todo;
